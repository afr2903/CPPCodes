#include<iostream>
#include<stdlib.h>
#include<time.h>
#include<windows.h>//system("cls");
#include<conio.h>//get&getch
using namespace std;
char mapa[10][10];
int main(){
    cout<<"     __     \n    _| |_    \n";
    /*int x,y;
    bool b=0;
    srand(time(NULL));
    for(int c=0;c<10;c++){
        for(int d=0;d<10;d++){
            if(b) mapa[c][d]=rand()%2;
            else mapa[c][d]=rand()%3;
            if(mapa[c][d]==2){
                b=!b;
                x=c;
                y=d;
                cout<<"o";
            }
            if(mapa[c][d]==1)cout<<"*";
            else cout<<".";
        }
        cout<<"\n";
    }
    cout<<x<<y;*/
}
